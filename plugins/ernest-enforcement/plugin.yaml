name: ernest-enforcement
version: 2.0.0
description: Draft-first gate — blocks live sends/publishes/CRM mutations (including Composio router calls) and enforces filesystem scope until the CEO approves.
author: Notiky
kind: standalone
provides_hooks:
  - pre_tool_call
